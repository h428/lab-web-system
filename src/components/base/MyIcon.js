import React from 'react';
import IconFont from '@/components/base/IconFont';

const LOGO = <IconFont type='icon-shaoping' />;
const SECURITY = <IconFont type='icon-security' />;
const EMAIL = <IconFont type='icon-email' />;
const PASSWORD = <IconFont type='icon-password' />;

const MyIcon = {
  LOGO,
  SECURITY,
  EMAIL,
  PASSWORD,
}

export default MyIcon;
